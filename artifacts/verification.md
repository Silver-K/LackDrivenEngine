# Godot 版本验证

## 按 test.md 整改生活机制

新增 `life.gd`，移除固定分钟轮换事件。人物由身体缺口、锚点张力、关系余波和熟悉应对产生生活片段；每晚使用不同随机种子，相同种子可重现以便诊断。没有任务完成分数或行动效用优化。

| 清单 | 操作与实际证据 | 判断范围 |
|---|---|---|
| 焦虑转移 | 陪伴后等待8分钟，十个夜晚均出现延迟羞耻余波 | 机制通过；表现是否自然仍需试玩 |
| 独立生活 | 离开10分钟，三名人物继续日常、创作、走神和相互触动 | 可见行为记录通过 |
| 不可刷 | 三种安抚各10次，共享递减并出现推拒动作 | 数值边界及动作通过 |
| 不可精确预测 | 十个种子操作相同，事件细节不同；固定种子可重现 | 情境变化通过，不等同于主观惊讶 |
| 矛盾自洽 | 推回照顾但保留椅子；合画册却不松手 | 动作冲突保留；LLM语义仍可能偏差 |
| 强迫性重复 | 返回后冷淡拒绝各角色4次，再离开观察30分钟，出现旧应对、后悔与日常交替 | 十个夜晚均出现，未注入心理数值 |

完整操作记录见 `life-playthrough.md`。51项已有核心测试通过，新增十夜41项检查通过。本机真实模型调用与原生渲染已验证。这里不声称自动测试代替了 `test.md` 要求的玩家实际感受评估。

## 统一表达流程更新（2026-09-16）

- 核心测试现为 51 项通过、0 项失败。新增公共场景记录、跨角色上下文、NPC 社交与自主表达、阅读间隔、离线不补播、生成中断恢复、事实人称保护、动作旁白拒绝等测试。
- 玩家对话、时间推进和静坐均接入 `expression_director.gd`。引擎继续独立推进人物心理；生成只提交显示台词。
- 本机真实模型实测：自由交谈、NPC 递茶回应、自主环境发言均返回真实台词；关键剧情生成未完整保留事实原句，被拒绝并回退到既定事实。测试报告保留失败结果，不将回退视为模型成功。
- 真实模型测试检查连接、三类表达提交、关键事实保留、心理状态不变和失败时的动作回退。模型输出存在随机性，测试通过不代表每一次生成都被接受，也不保证消除所有语义矛盾。
- 原生窗口渲染检查通过（`SMOKE_UI_OK`）。已有玩家存档及模型设置未重置，旧对话日志保留；重新启动游戏即可加载新流程。

以下为上一版的验证记录：

验证日期：2026-09-16。运行环境：Windows，Godot 4.6 stable，原生 OpenGL 场景。

- 心理引擎与边界测试：32 项通过，0 项失败。覆盖匮乏下限、安抚递减及副作用上限、歧义输入、离场后的自主生活、同层防御共存、故事揭示和信件、存档恢复后继续推进时间、模型越权和玩家台词复述拒绝、环境密钥的服务地址限制。
- 真实 LLM：本机 Ollama / llama3.1:8b 完成连接、剧情表达、自由对话，以及 Chat Completions 兼容接口测试。生成前后心理引擎状态一致。结果见 `llm-test.json`。
- 实际窗口交互：点击陪伴后显示等待状态，返回「你坐得舒服吗？」并标注 `LLM · llama3.1:8b`，按钮恢复可用。原生窗口运行错误日志为空。
- 设置窗口：服务地址、模型、字号可见；滚动后保存、存档目录、重新开始按钮均可访问。
- 画面检查：1440×900、1180×760、设置与开场均已渲染并检查，截图保存在本目录。

限制：远端付费服务未实测；模型自然语言检查不保证排除所有隐性幻觉。此版本为可运行的单场景章节工程，未打包商业发行安装程序。
