# 单主体实验与模块化架构

先读 [共识](docs/CONSENSUS.md)、[模块地图](docs/MODULES.md)、[场景实验协议](docs/MINIMAL_ROOM.md)。AGENTS.md 要求后续改动延续这些边界。

运行路径：物理装置 → 主体感受 → 通用引擎执行主体状态规则 → 来源独立的贡献 → 身体合成、承担对抗、物理接触 → 主体反馈映射与关联更新。

`tension_core.gd` 没有改成场景专用核心。具体四维耦合、可塑性和感受仍在 `subjects`；身体与传播几何在 `runtime`。`physical_world.gd` 装配这些模块；新增 `room_experiment.gd` 控制环境阶段及完整历史，`room_view.gd` 提供动画、观察和操作。

场景只初始化一个零环境关联主体。它保留感受映射和内部耦合结构，但没有对象接近／远离的强先验。感受差须先改变张力；已删除新异直接推进、独立随机转向及休眠的直接吸引路径。实验控制器不决定行动，不设置目的地，也不写入心理状态。阶段变化不重置身体。

贡献保留每个维度、关联和来源。身体用作用长度之和减净作用长度计算对抗负荷，形成阻尼及余波，主体结构定义反馈。此公式属于当前身体，并非通用数字生命定义。

旧动物、酒馆与网页场景已删除，启动入口直接加载最小房间。存档使用独立的 `tension-room` 格式版本 9，完整保存装置、主体、历史，不迁移含旧驱动的历史。

游戏目前只装配一种二维身体。引擎可替换结构的能力由独立的二维、七维测试结构验证。当前自由生活结果和时序关联实验不能代替真实动物行为验证。

运动结构位于 `subjects/motor_dynamics.gd`；逐维张力推进与张力变化转向分通道进入身体。固定相位探索、墙边预推力、切线导航及独立运动波动已移除。身体保留朝向和角速度，并通过真实接触的法向冲量重定向。原有运动与适应参数未为补偿活动量而提高。

低活动由身体的连续执行幅度与使用适应反馈形成，已删除常量推进并保留逐维作用符号。没有休息目标或定时停留。界面显示执行幅度与适应负荷；旧运动覆盖对照是历史版本，当前观察见 `artifacts/quiescence-observations.json`。

`planar_senses.gd` 与 `planar_self_senses.gd` 分别生成外部和可选自身样本。世界只提供身体快照，当前主体定义决定是否映射其中的物理量。样本来源保留在贡献记录中。

`planar_events.gd` 将主体许可的连续变化转换成独立模式激活与上升沿事件。事件层保留来源和迟滞状态；它不按外部或自身来源默认授予学习权限。

多个特征模式可同时激活、独立释放，各自的关联和逐维贡献保留到身体合成。观察只读取当步快照；冻结探测与开启可塑性的消退使用不同测试协议，详见场景实验文档。
