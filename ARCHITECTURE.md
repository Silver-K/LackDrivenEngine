# 模块化架构 v4

## 先读共识

- [已达成的架构共识](docs/CONSENSUS.md)：张力定义、可塑性、感受映射属于主体；引擎保留贡献；身体承担矛盾。
- [模块地图与接口](docs/MODULES.md)：代码位置、数据协议、依赖方向、验证入口及限制。
- 根目录 AGENTS.md 要求后续修改前先读以上文件，避免上下文压缩后退回旧方案。

## 当前运行路径

物理快照 → 主体感受 → 通用引擎调用主体状态规则 → 来源独立的贡献集合 → 主体身体合成/承担负荷 → 物理接触 → 主体反馈映射与学习 → 新状态。

通用引擎 `tension_core.gd` 不再持有四维公式或关联学习。原型规则集中在 `subjects/planar_structure.gd`、`planar_senses.gd` 和 `trace_plasticity.gd`。身体执行位于 `runtime/body_physics.gd`，世界组装位于 `physical_world.gd`。

## 对抗的实现

贡献保留来源索引、通道和方向/量，包括每个维度与每条关联。身体才计算合力。当前二维身体用作用长度之和减净作用长度计算对抗负荷，形成阻尼及可恢复余波；主体配置决定其如何映射回内部维度。这个规则只属于当前身体，不是数字生命的普遍定义。

## 成长

仍有三个带先验的母体与三个零环境关联幼体。关联学习属于这个主体家族，可关闭或由另一结构实现替代；通用引擎不优化关联。时间不直接注入经验。保存经历支持版本4，新架构与旧版本3存档不自动混用。

## 验证

`test_modules.gd` 覆盖不同维数/规则、状态隔离、拓扑验证、相反贡献保留、零合力与零作用的差异、主体控制反馈和学习。其余测试覆盖无语义渗透、配对/错时/消退、存档与实际界面。

当前仅有一种用于游戏的二维主体家族。通用引擎接口已用另一测试结构验证，不意味着已有任意身体的完整游戏支持。具体物理规则、感受及积分公式仍由当前适配器实现。
