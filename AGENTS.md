# 项目架构约束

开始修改心理、学习、身体或环境模块前阅读 `docs/CONSENSUS.md` 和 `docs/MODULES.md`。它们记录用户已认可的架构边界，不要凭旧对话摘要回退。

通用引擎禁止固定维数、物种/心理/动作类别、学习规则或提前合力。主体规则属于 subjects，物理合成属于 runtime，显示属于 animal_world/pet_house。保留贡献来源和对抗负荷。

修改对应模块后运行 `test_modules.gd` 及相关主体/存档/UI测试。历史 artifacts 不是当前验收依据。不要把历史原型称为普遍生物规律。
